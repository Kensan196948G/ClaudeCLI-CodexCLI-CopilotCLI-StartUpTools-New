# 区切りサマリ

2026年3月13日時点の区切りです。

## 今回まとまった範囲

- `Start-All.ps1` に Agent Teams lane 表示を追加
- `Start-Menu.ps1` に recent filter / search / sort を追加
- launch metadata の日次ローテーションと集計を追加
- `Test-AllTools.ps1` に MCP startup / shutdown / timeout / runtime probe を追加
- `Test-McpRuntime.ps1` を追加して CI と実機チェックを分離
- `Update-TASKS.ps1` / `Sync-AgentTeamsBacklog.ps1` を backlog manager として拡張
- `test-drive-mapping.ps1` に `net use` 再接続候補を追加
- Windows Terminal profile override を拡張
- Pester を 108 件まで拡張

## 推奨コミット分割

1. `launcher: Agent Teams dashboard and recent project flows`
2. `diagnostics: MCP runtime probe and drive mapping recovery`
3. `backlog: task tooling and Agent Teams sync rules`
4. `setup: windows terminal profile overrides`
5. `docs/test: operating boundary, runtime guide, and regression coverage`

## コミット対象の目安

### 1. launcher

- `scripts/main/Start-All.ps1`
- `scripts/main/Start-Menu.ps1`
- `scripts/lib/LauncherCommon.psm1`
- `scripts/lib/MenuCommon.psm1`

### 2. diagnostics

- `scripts/test/Test-AllTools.ps1`
- `scripts/test/Test-McpRuntime.ps1`
- `scripts/test/test-drive-mapping.ps1`
- `docs/common/schemas/test-all-tools-report.schema.json`

### 3. backlog

- `scripts/tools/Update-TASKS.ps1`
- `scripts/tools/Sync-AgentTeamsBacklog.ps1`
- `config/agent-teams-backlog-rules.json`
- `config/agent-teams-backlog-rules.json.template`
- `TASKS.md`

### 4. setup

- `scripts/setup/setup-windows-terminal.ps1`
- 関連する README / docs のセットアップ記述

### 5. docs/test

- `README.md`
- `config/README.md`
- `docs/common/08_AgentTeams対応表.md`
- `docs/common/09_CIと実機ヘルスチェック.md`
- `tests/Diagnostics.Tests.ps1`
- `tests/StartScripts.Tests.ps1`

## 残課題

- 実 MCP 設定 `.mcp.json` をまだ置いていない
- Agent Teams の実ランタイムは未導入
- `powershell.exe` と `pwsh` の実行互換差は残っている
