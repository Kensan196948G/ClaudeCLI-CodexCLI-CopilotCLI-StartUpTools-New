# 経路異常時の段階的再試行

- 種別: 機能アイデア（未実装）
- 前提: 固定PC運用（Windows 11 → Ubuntu SSH）、管理者権限でバッチ実行、ネットワーク常時接続、ChromeDevTools利用可能
- 目的: DNS・SSH・DevToolsを切り分けた再試行戦略を導入する。
- 期待効果: 障害原因ごとに最短の復旧手段を選べる。
- アイデア詳細: 一律リトライではなく障害層別の対処へ切り替える。
- 実装段階: 企画/検討中（実装はまだ行わない）
