# ネットワーク断検知時のソフトランディング

- 種別: 機能アイデア（未実装）
- 前提: 固定PC運用（Windows 11 → Ubuntu SSH）、管理者権限でバッチ実行、ネットワーク常時接続、ChromeDevTools利用可能
- 目的: 常時接続前提が崩れた際の安全停止フローを定義する。
- 期待効果: 中途半端な状態を残さず復旧しやすくなる。
- アイデア詳細: 復旧可能状態を保存して次回起動時に再開できる形を目指す。
- 実装段階: 企画/検討中（実装はまだ行わない）
