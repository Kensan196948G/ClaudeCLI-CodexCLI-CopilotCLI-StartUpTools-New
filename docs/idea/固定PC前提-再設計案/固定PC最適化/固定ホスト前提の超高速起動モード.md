# 固定ホスト前提の超高速起動モード

- 種別: 機能アイデア（未実装）
- 前提: 固定PC運用（Windows 11 → Ubuntu SSH）、管理者権限でバッチ実行、ネットワーク常時接続、ChromeDevTools利用可能
- 目的: 接続先が固定である前提を活かし、検証手順を段階的に短縮する。
- 期待効果: 日次起動の待ち時間を大幅に減らせる。
- アイデア詳細: 前回正常状態をキャッシュし、差分チェックのみ実施する。
- 実装段階: 企画/検討中（実装はまだ行わない）
