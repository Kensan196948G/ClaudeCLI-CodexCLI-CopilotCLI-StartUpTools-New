# 必須前提チェックダッシュボード

- 種別: 機能アイデア（未実装）
- 前提: 固定PC運用（Windows 11 → Ubuntu SSH）、管理者権限でバッチ実行、ネットワーク常時接続、ChromeDevTools利用可能
- 目的: ChromeDevTools利用可否を中心に、SSH・ポート・権限の前提を一覧表示する。
- 期待効果: 運用開始前の判断が即時にできる。
- アイデア詳細: OK/NGだけでなく、復旧コマンドを同時表示する。
- 実装段階: 企画/検討中（実装はまだ行わない）
