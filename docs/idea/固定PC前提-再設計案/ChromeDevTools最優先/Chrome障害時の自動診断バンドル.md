# Chrome障害時の自動診断バンドル

- 種別: 機能アイデア（未実装）
- 前提: 固定PC運用（Windows 11 → Ubuntu SSH）、管理者権限でバッチ実行、ネットワーク常時接続、ChromeDevTools利用可能
- 目的: ChromeDevTools失敗時に必要ログを自動収集して1つにまとめる。
- 期待効果: 障害解析の初動を短縮できる。
- アイデア詳細: ブラウザ起動ログ、ポート状態、SSH接続結果を同梱する。
- 実装段階: 企画/検討中（実装はまだ行わない）
